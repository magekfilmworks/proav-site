version: 1
frontend:
  phases:
    preBuild:
      commands: []
    build:
      commands: []
  artifacts:
    baseDirectory: /
    files:
      - 'index.html'
      - 'contact.html'
      - 'robots.txt'
      - 'sitemap.xml'
      - 'og-image.png'
      - 'images/**/*'
  cache:
    paths: []
